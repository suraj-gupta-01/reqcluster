# ReqCluster Configuration Template
# Copy this file to .env and fill in your details.

# ==========================================
# Database (Bottleneck #1 — SQLite → PostgreSQL)
# ==========================================
# Set DATABASE_URL to use PostgreSQL (strongly recommended for production).
# If this variable is NOT set, ReqCluster falls back to SQLite
# (data/reqcluster.db) — local dev only.  A prominent warning will be logged
# at startup to remind you that SQLite is active.
#
# PostgreSQL (Docker Compose, adjust host/port/credentials as needed):
#   DATABASE_URL=postgresql://reqcluster:reqcluster_dev_password@localhost:5432/reqcluster
#
# To explicitly acknowledge SQLite usage and silence the warning:
#   REQCLUSTER_SQLITE_OK=1
#
# DATABASE_URL=postgresql://user:password@localhost:5432/reqcluster

# ==========================================
# Redis — per-text embedding cache (Bottleneck #3)
# ==========================================
# Optional but strongly recommended for datasets > 5 K requirements.
# When set, individual text embeddings are cached in Redis (30-day TTL)
# so repeated clustering runs avoid re-encoding unchanged texts.
# The app works fine without Redis — it falls back to the .npy file cache.
#
# REDIS_URL=redis://localhost:6379

# Provider selection: "mock" (offline/deterministic), "openai" (OpenAI-compatible cloud), or "local" (on-premise model)
REQCLUSTER_LLM_PROVIDER=mock

# ==========================================
# OpenAI / OpenAI-Compatible Configuration
# ==========================================
# Base URL for the OpenAI-compatible API (e.g., Groq, OpenRouter, OpenAI, etc.)
# For Groq, use: https://api.groq.com/openai/v1
# For OpenAI, use: https://api.openai.com/v1
REQCLUSTER_LLM_BASE_URL=https://api.groq.com/openai/v1

# API Key for the selected provider
REQCLUSTER_LLM_API_KEY=your_api_key_here

# Model name to use
# For Groq, e.g.: llama-3.3-70b-versatile, mixtral-8x7b-32768
# For OpenAI, e.g.: gpt-4o-mini
REQCLUSTER_LLM_MODEL=llama-3.3-70b-versatile

# Optional settings
REQCLUSTER_LLM_TIMEOUT_SECONDS=30.0
REQCLUSTER_LLM_MAX_RETRIES=2

# ==========================================
# Local LLM Configuration (e.g., Ollama, vLLM)
# ==========================================
# Endpoint for your locally hosted LLM (use /api/generate for Ollama native)
REQCLUSTER_LOCAL_LLM_URL=http://localhost:11434/api/generate
REQCLUSTER_LOCAL_LLM_MODEL=qwen2.5-coder:7b
REQCLUSTER_LOCAL_LLM_TIMEOUT_SECONDS=30.0
